return {
	"terrortylor/nvim-comment",
	config = function()
		require("nvim_comment").setup({
			hook = function()
				local filetype = vim.bo.filetype
				if filetype == "dart" then
					vim.opt_local.commentstring = "// %s" -- or any other format you prefer
				end
			end
		})
	end
}
